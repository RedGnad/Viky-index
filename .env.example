# HyperSync and the hosted service need an Envio API token (https://envio.dev/app/api-tokens). The token is a
# credential: it lives in .env, never in the repository. config.rpc.yaml runs the same indexer without one.
ENVIO_API_TOKEN=
